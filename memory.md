## RAYCOM – Project Memory

**Περιγραφή έργου**
- Εργαλείο διπλωματικής βασισμένο στο Sionna (RT + channel simulation) και Mitsuba rendering.
- Επιτρέπει προσθήκη 3D αντικειμένων (STL/OBJ) με custom radio materials (`RadioMaterial`) μέσα σε scenes Sionna, και μελέτη του πώς τα υλικά επηρεάζουν την ασύρματη ζεύξη.
- Χρησιμοποιεί κλάσεις `sionnaObject` και `SionnaEnv` στο `Raycom/raycom-tool.py` για φόρτωση σκηνών, προσθήκη αντικειμένων/υλικών, υπολογισμό καναλιού, στατιστικών και rendering.

**Τρέχων στόχος επέκτασης**
- Υποστήριξη πολλών receivers (π.χ. πλέγμα 5x5) ώστε από τα λαμβανόμενα σήματα να παράγονται RF heatmaps.
- Δημιουργία dataset ζευγών εικόνων (RF heatmap → Optical render) για εκπαίδευση μοντέλου τύπου pix2pix, ώστε να ανακατασκευάζεται η τελική εικόνα του χώρου χωρίς χρήση του Mitsuba renderer.
- Στον κώδικα υπάρχει ήδη δημιουργία heatmaps και dataset (π.χ. `generate_heatmap`, `save_radio_heatmap`, `save_dataset_pair`, `generate_dataset_example1`).

**Σημειώσεις για το πρόβλημα με μαύρες εικόνες**
- Τα RF heatmaps παράγονται σωστά (εικόνες Α του dataset).
- Οι “τελικές” εικόνες (εικόνες Β που προέρχονται από `scene.render_to_file` μέσα στη `save_dataset_pair`) βγαίνουν μαύρες.
- Η τρέχουσα λογική: η κάμερα τοποθετείται κοντά στον receiver (router) και κοιτάει προς τον transmitter (laptop). Αν η κάμερα βρίσκεται μέσα στο mesh του router ή σε σκοτεινό / κλειστό σημείο, το Mitsuba μπορεί να δώσει μαύρο output.

**Αρχές συνεργασίας με τον agent**
- Να διατηρείται πάντα η υπάρχουσα CLI λειτουργικότητα (`raycom-tool.py` με τα flags `--example*`, `--gen_dataset`, `--config`).
- Να μην αλλάζει η φυσική του καναλιού (υλικά, θέσεις, υπολογισμοί) εκτός αν το ζητήσει ρητά ο χρήστης.
- Όταν γίνονται αλλαγές, να εντοπίζονται και να σχολιάζονται κυρίως σε συναρτήσεις:
  - `SionnaEnv.generate_heatmap`, `plot_radio_image`, `save_radio_heatmap`
  - `SionnaEnv.save_dataset_pair`
  - `generate_dataset_example1`
- Πριν από οποιαδήποτε ενέργεια (εντολές, αλλαγές κώδικα, debugging βημάτων) ο agent πρέπει νοητά να λαμβάνει υπόψη του το περιεχόμενο του `memory.md`.

**Επόμενα βήματα (υψηλού επιπέδου)**
- Σταθεροποίηση παραγωγής οπτικών εικόνων (εικόνες Β) έτσι ώστε να μην είναι μαύρες:
  - Ρύθμιση θέσης κάμερας σε ασφαλές σημείο εκτός αντικειμένων & σωστό `look_at`.
  - Προαιρετικά: χρήση μιας σταθερής “preview” κάμερας ανά σκηνή για έλεγχο.
- Προετοιμασία dataset σε μορφή συμβατή με pix2pix:
  - Κανονικοποίηση εικόνων (π.χ. [0,1] ή [-1,1]) και σταθερή ανάλυση (π.χ. 256×256).
  - Σωστό pairing A/B (RF → Optical) στα αντίστοιχα subfolders.

